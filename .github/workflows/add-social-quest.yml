name: Social Quest Assets

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Social Quest is managed in source
        run: echo "Social Quest layout and links are now maintained directly in assets/js/social-quest.js."
